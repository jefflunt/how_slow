Slowest <%= @number_of_actions %> action(s), sorted by <%= @action_sort_by.to_s.gsub(/_/, " ").downcase %>:
--------------------
<% @action_metrics.each do |am| %>
  <%= "#{am.datetime} :: #{am.params['controller']}/#{am.params['action']}\t#{am.total_runtime.to_i} / #{am.db_runtime.to_i} / #{am.view_runtime.to_i}"}
<% end %>


Counters sorted by <%= @counter_sort_by.to_s.gsub(/_/, " ").downcase %>:
--------------------
<% @counter_metrics.each do |cm| %>
  <%= "#{am.event_name} :: #{am.count}" %>
<% end %>
